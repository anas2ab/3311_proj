add_class("A")
add_attribute("A", "i", "INTEGER")
add_command("A", "set_i", <<["x", "INTEGER"], ["y", "INTEGER"]>>)

add_assignment_instruction("A", "set_i", "i")

multiplication

addition -- should show addition first and then multiplication but shows multiplication first

int_value(1)

int_value(2)

int_value(3)

add_assignment_instruction("A", "set_i", "i")

quotient

add_call_chain(<<"i">>)

subtraction -- this is wrong too


add_call_chain(<<"x">>)

add_call_chain(<<"y">>)

generate_java_code
